source 'https://rubygems.org'

gem 'thin' # web server
gem 'eventmachine' # for thin
gem 'sinatra' # simple web application server
gem 'sinatra-contrib' # for hot reload
gem 'rackup' # for running the server
